apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: wabba-server
  name: wabba-server
  namespace: wabba-server
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: wabba-server
  type: LoadBalancer
